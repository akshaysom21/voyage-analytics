version: '3.9'

services:
  flight-api:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    container_name: voyage-flight-api
    ports:
      - "5000:5000"
    restart: unless-stopped
